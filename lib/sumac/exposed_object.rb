module Sumac

  # Class that represents all objects that are exposed ({LocalObject}s and {RemoteObject}s).
  # Exists only so the local application can check if the object is exposed by type checking.
  module ExposedObject

  end

end